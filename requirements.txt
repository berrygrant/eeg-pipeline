# Convenience requirements for a full CPU runtime (core + visualization).
#
# pyproject.toml is the authoritative dependency contract (with version
# bounds). Prefer installing the package with extras instead:
#     pip install ".[viz]"            # core + visualization
#     pip install ".[dev,viz]"        # add test/lint toolchain
#     pip install ".[gpu]"            # CUDA-compatible CuPy build
#     pip install ".[ica]"            # python-picard ICA solver

# Core
mne
numpy
scipy
pandas
pyarrow
PyYAML

# Visualization (optional extra: .[viz]); included here for convenience
matplotlib
seaborn
