"""
eeg_pipeline package.

Invoke CLI with:
  python -m eeg_pipeline.cli ...
"""

# Single source of truth for the package version. pyproject.toml reads this
# via [tool.setuptools.dynamic] (dynamic = ["version"]).
__version__ = "2.2.0"
